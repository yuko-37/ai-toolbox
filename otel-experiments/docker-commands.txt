docker run -d --name otel-collector --network observability \
    -p 4317:4317 \
    -v $(pwd)/collector-config.yaml:/etc/otelcol/config.yaml otel/opentelemetry-collector:latest

docker run -d --name jaeger --network observability \
  -e COLLECTOR_OTLP_ENABLED=true \
  -p 16686:16686 \
  jaegertracing/all-in-one:latest
